import { SvgtextfitDirective } from './svgtextfit.directive';

describe('SvgtextfitDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgtextfitDirective();
    expect(directive).toBeTruthy();
  });
});
