import { IsNumberChangeDirective } from './is-number-change.directive';

describe('IsNumberChangeDirective', () => {
  it('should create an instance', () => {
    const directive = new IsNumberChangeDirective();
    expect(directive).toBeTruthy();
  });
});
