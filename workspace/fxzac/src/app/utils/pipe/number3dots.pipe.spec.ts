import { Number3dotsPipe } from './number3dots.pipe';

describe('Number3dotsPipe', () => {
  it('create an instance', () => {
    const pipe = new Number3dotsPipe();
    expect(pipe).toBeTruthy();
  });
});
